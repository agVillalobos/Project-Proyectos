service: proyectos-api

provider:
  name: aws
  runtime: nodejs6.10
  memorySize: 128
  timeout: 10
  stage: dev
  region: us-east-1

functions:
  create:
    handler: controllers/project.create
    events:
      - http:
          path: notes
          method: post
          cors: true
  getOne:
    handler: controllers/project.getOne
    events:
      - http:
          path: notes/{id}
          method: get
          cors: true
  getAll:
    handler: controllers/project.getAll
    events:
     - http:
         path: notes
         method: get
         cors: true
  update:
    handler: controllers/project.update
    events:
     - http:
         path: notes/{id}
         method: put
         cors: true
  delete:
    handler: controllers/project.delete
    events:
     - http:
         path: notes/{id}
         method: delete
         cors: true

plugins:
- serverless-offline